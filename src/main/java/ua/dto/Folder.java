package ua.dto;

/**
 * 
 * Enom is needed to create the folder images 
 * and in it the folder NEW_IMAGE
 * (in lowercase) where we will save the picture 
 * with the shown differences
 *
 */
public enum Folder {
	NEW_IMAGE
}
